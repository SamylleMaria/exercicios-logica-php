# 🐘 PHP Playground com Docker

Projeto simples para rodar exercícios de PHP usando Docker.

## 🚀 Como rodar (Docker instalado)

### 1. Subir o container

```bash
docker-compose up --build
```
### 2. Acessar no navegador
```bash
http://localhost:8000
```
